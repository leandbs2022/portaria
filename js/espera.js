


/*
browser.idle.onStateChanged.addListener(newState);

function newState(state) {
    console.log(`New state: ${state}`);
  }


function stardby(espera) {
    //if (duration == 300 || espera == 1) { window.open('index.php', _self) }
}
function startTimer() {
    var duration = 0
    let time = 0
    let tempo = document.getElementById('tempo')
    setInterval(function () {

        let data = new Date();
        time = data.getSeconds()

        if (time == 59) {
            duration = duration + 60
            espera = 1
        }

        if (espera = 0) {
            duration = 0
        }
        tempo.innerHTML = time + ' - ' + duration + ' - ' + espera
    }, 1000);
}*/
